{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "devCommand": "cd frontend && npm run dev",
  "installCommand": "cd frontend && npm install",
  "framework": "vite",
  "_comment": "⚠️ IMPORTANTE: Si usas rewrites, actualiza 'destination' con tu URL real de Railway después del deployment. Mejor usar VITE_API_URL en variables de entorno."
}

